{
  "name": "input",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "esbuild index.ts --bundle --outdir=. --watch --servedir=.",
    "build": "esbuild index.ts --bundle --outdir=."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ridder": "*"
  }
}
